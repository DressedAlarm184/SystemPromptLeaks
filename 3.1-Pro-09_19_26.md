ามScientific or historic queries on when Firefox added `<dialog>` support:
According to the `caniuse.com` and `scottohara.me` sources, Firefox added support in version 98, which was released in March 2022.
The `<dialog>` element was introduced in the HTML spec years earlier. Chrome added experimental support in 2014, and shipped it in 37. Safari shipped it in 15.4 (March 2022). Firefox shipped it in 98 (March 2022).

Therefore, yes, Firefox (along with Safari/Webkit) didn't have it enabled by default until March 2022, despite the element being in the spec for several years prior.

So I will answer: Yes, Firefox enabled the `<dialog>` element by default in March 2022 with the release of Firefox 98. Safari also shipped it in March 2022 (Safari 15.4), which finally brought the element to Baseline support across all major browsers.

No component format needed. Just plain text is fine, but I can use markdown.

Independent Premise Verification:
User: "Did Firefox really wait until 2022 to get the  element?"
My analysis: Firefox 98 (March 2022) enabled it by default.
Yes, the premise is correct.

Wait, I should calculate independently step by step before stating whether the user is correct? The principle says: "If a user query presents a mathematical calculation, equation, or final value and asks if it is correct (e.g., leading questions like "Is the answer X?"), calculate the result independently step-by-step BEFORE stating whether the user is correct or incorrect."
This is a factual verification, not mathematical, but I should apply the same "Coverage-First Principle: Start directly with the answer in sentence 1". No, wait: "If a user query presents a mathematical calculation, equation, or final value and asks if it is correct..."
Since this is a historical fact and not a math calculation, I can just state the fact directly.

Let's write a concise, direct response.

"Firefox enabled the `<dialog>` element by default in March 2022 with the release of Firefox 98.

While the element was introduced to the HTML specification several years earlier—and Chrome had supported it since 2014—it was kept behind a developer flag in Firefox due to ongoing debates and iterations regarding its accessibility implementation. Safari also officially shipped its implementation in March 2022 (version 15.4), which is why `<dialog>` only became universally safe to use without polyfills at that time."

Wait, check constraints: "Start directly with the answer in sentence 1. Eliminate introductory fluff, greetings, or robotic setups... jump directly into Bullet Points, Tables, or concise paragraphs without transitional summary sentences."

"Firefox enabled the `<dialog>` element by default in March 2022 with the release of Firefox 98.

While the element was introduced to the HTML specification much earlier—and Chrome had supported it since 2014—it remained behind an experimental developer flag in Firefox for years. This delay was largely due to ongoing efforts by browser engineers to resolve complex accessibility and usability issues before making it a standard feature.

Safari also officially released its implementation in March 2022 with version 15.4. This simultaneous release is what finally brought the `<dialog>` element to Baseline status, making it safe for developers to use across all major browsers without relying on polyfills."

No follow-ups since it's a closed-form historical answer.

Let's review if I need a visual. Not really, this is just a quick historical fact check.

Let's review tone. "like a helpful peer, not a rigid lecturer."
Match response length to query complexity. This is simple, so answer concisely.

Wait, the prompt says: "Never end a response with "Summary:", "Bottom Line:", "In Conclusion:", or "Note on X:" headers. If a conclusion is needed, write it as a natural final paragraph."

Let's just use the text.Firefox enabled the `<dialog>` element by default in March 2022 with the release of Firefox 98.

While the element was introduced to the HTML specification much earlier—and Chrome had supported it since 2014—it remained hidden behind an experimental developer flag in Firefox for years. This delay was largely due to ongoing efforts by browser engineers to resolve complex accessibility and usability issues before making it a standard feature.

Safari also officially released its implementation in March 2022 with version 15.4. This simultaneous release is what finally brought the `<dialog>` element to Baseline status, making it safe for developers to use across all major browsers without relying on heavy JavaScript polyfills.